<%= Admin.count %>
<% Admin.all.each do |a| %>
	<%= a.inspect %><br>
<% end %>

<h2>Admin Login</h2>

<%= form_tag(admin_sessions_path, method: :create) do %>

  Name:<br>
  <%= text_field :session, :name, required: true %>
  <br>
  password:<br>
  <%= password_field :session, :password, required: true  %>

  <br><br>
  <button type='submit'>Login</button>
<% end %>

<br>
<hr><br>
<h2>Create Admin</h2>
<%= render 'new_admin' %>

